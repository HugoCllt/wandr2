# TBD ledger

Deferrals captured per `CLAUDE.md` §3.1. Nothing speculative ships in code; everything parked here gets picked up when its consumer exists. One bullet each.

## Assumptions

- **Single city / single user POC.** `User.cityId` and `Activity.cityId` are required; the feed is scoped to the connected user's city. Multi-city is out of scope. — spec `2026-05-23-activities-db-ingestion-design.md` §1.
- **Montréal bbox is approximate** (`lat 45.40–45.71`, `lng -73.98 to -73.47`). Tighten with real bounds before relying on it to reject Tavily results. — `prisma/seed.ts`, `City` model.
- **`dedupeKey` uses UTC day + 3-decimal coord rounding (~100 m).** Fuzzy/spelling-variant matching deferred. — `computeDedupeKey.ts`, spec §5/§9.
- **Event expiry compares in UTC.** `City.timezone` is stored but unused until tz-aware "is it past?" logic is needed. — spec §4.1.
- **No authorization on the ingestion MCP server.** stdio = OS-process isolation, no network surface to protect (spec plan-2 Q4). Revisit (bearer token) only if the server is ever exposed over HTTP/remote. — `src/mcp/server.ts`.
- **Dedicated Prisma client in `src/mcp/db.ts`.** The MCP process does not reuse the shared `src/shared/db/prisma.ts` singleton because its string-form `log` writes to stdout (corrupts JSON-RPC). Reconcile the two clients if/when the `apps/web` + `apps/mcp` split happens. — `src/mcp/db.ts`, spec plan-2 §3.
- **Feed ranking is a weighted average of affinity over the category set** (primary ×1, each secondary ×0.5, normalized by `1 + 0.5·|secondary|`). Anonymous users (empty affinity) score `DEFAULT_MATCH_SCORE` regardless of set size. — `src/modules/feed/application/ranking/p1.ts`, spec `2026-05-25-multi-category-activities-design.md` §2.
- **Cross-scout `DUPLICATE` keeps the first ingester's category set** (no merge); `computeDedupeKey` ignores category, so a duplicate only refreshes freshness. Cross-scout consistency is steered by the scout prompt's classification rule, not by code. — `PromoteCandidateUseCase.ts`, `.claude/agents/wandr-theme-scout.md`, spec §6 decision #6.
- **`cursor-codec` `matchScore` is a bounded float** (`z.number().min(0).max(10)`, was `.int()`) to carry the weighted score; the same computed float is the sort key and the cursor key. — `src/modules/feed/application/cursor-codec.ts`, spec §2.
- **Seed upserts on `cityId_dedupeKey`** (since `externalId` was dropped); `dedupeKey` is computed in the loop. — `prisma/seed.ts`, spec §5.
- **Reseed nukes the seed user's data.** `prisma/seed.ts` deletes the seed user (and cascades Account/Session, plus its favorites/calendar/affinities) then recreates it through `auth.api.signUpEmail` so it has a real credential Account. POC only — no prod data. — `prisma/seed.ts`.
- **Schema applies via `prisma db push`, not migrations.** No `prisma/migrations/` dir exists; the Better Auth tables + User profile fields land on the next `pnpm db:push` / `db:migrate` against a live DB. Adopt a migrations baseline if/when a non-disposable DB exists. — `prisma/schema.prisma`.
- **Lenis smooth scroll + the hero-parallax / rail scroll-reveal are scoped to the `(with-sidebar)` group.** `SmoothScroll` is mounted in that layout, so Home / category / favorites / calendar get momentum scrolling and the effects; chat / profile / login keep native scroll. The rail reveal is desktop-only (≥1200px) — the mobile drawer + toggle is unchanged. — `src/app/(with-sidebar)/layout.tsx`, `src/shared/ui/SmoothScroll.tsx`.
- **Chat messages are not persisted — the client owns the visible thread.** `SendChatMessageUseCase` is stateless (only token usage is written to DB); the browser keeps the conversation in React state and replays it each turn. Add a `ChatThread`/message store if cross-device history or server-side moderation is ever needed. — `src/modules/chat/web/ChatPage.tsx`, `src/modules/chat/application/SendChatMessageUseCase.ts`.
- **Chat history is supplied by the client and trusted as-is** (capped at 50 turns, role+text only). The server does not reconcile it against a stored thread. Revisit if abuse (prompt stuffing / spoofed assistant turns) becomes a concern. — `src/modules/chat/web/chatMessagesRoute.ts`.
- **`CHAT_THINKING_EFFORT` only applies to the Ollama provider.** It's passed as `reasoning_effort` in `modelKwargs` on the Ollama branch of `createChatModel`; the OpenRouter branch ignores it (`'none'` is not a standard OpenRouter/OpenAI value and would 400). Add a provider-appropriate thinking control if `CHAT_LLM_PROVIDER=openrouter` ever needs one. — `src/modules/chat/infra/createChatModel.ts`, `src/shared/config/env.ts`.

## Future changes

- **Email verification.** Better Auth is ready (`emailAndPassword.requireEmailVerification` + `sendVerificationEmail`); left off for the POC. `User.emailVerified` exists but is only set by OAuth. Wire when an email provider (e.g. Resend) is configured. — `src/shared/auth/auth.ts`.
- **Password reset flow.** Better Auth is ready (`emailAndPassword.sendResetPassword`); not wired for the POC (no email provider). — `src/shared/auth/auth.ts`.
- **Custom avatar upload.** Avatar is the Google `image` when present, else generated initials (`shared/ui/Avatar.tsx`). No upload surface. — `src/modules/profile/web/ProfilePage.tsx`.
- **Surface computed age / gender on the profile.** `PrismaProfileRepository` reads `name`/`bio`/`image` + affinities, but `gender`/`birthDate` (now editable via the onboarding/edit form) are not yet shown on `ProfilePage`. Add an age/gender line if the design wants it. — `src/modules/profile/infra/PrismaProfileRepository.ts`.
- **Wire `QUICK_ACTIONS` hrefs on `ProfilePage`.** Still hardcoded buttons with no navigation (Favorites → `/favorites`, History → `/calendar`, etc.). — `src/modules/profile/web/ProfilePage.tsx`.
- **Expose city in `ActivityDTO`** (`{ slug, name }`, not the opaque `cityId`) once a multi-city UI / city switcher exists. Today the city flows server-side from the connected user; the DTO does not carry city at all.
- **Attribute `UserCategoryAffinity` to the primary category** when user actions (favorite/calendar) start writing affinity. No code writes affinity today (only `seed.ts`), so this is parked until a writer exists. — spec `2026-05-25-multi-category-activities-design.md` §6.
- **Split monorepo `apps/web` + `apps/mcp`.** The ingestion MCP server ships in `src/mcp/` for now (spec plan-2 Q2); reconcile with `CLAUDE.md` §4's `apps/web` naming when a real second deploy target exists. — `src/mcp/`.
- **Rename `listActivitiesDueForRecheck` → `listPlacesDueForRecheck`.** Today it only ever returns PLACEs (EVENTs have no `recheckAfter`); keep the generic name unless EVENT re-verification is ever added, then rename for clarity. — `src/mcp/tools/listActivitiesDueForRecheck.ts`.
- **Return `structuredContent` from the MCP tools.** Currently tools return JSON in a text content block (uniform across tools; the array-returning `listActivitiesDueForRecheck` would otherwise need an `{items:[]}` wrapper). Add `structuredContent` + `outputSchema` if a client benefits from typed structured output. — `src/mcp/runTool.ts`.
- **Extract `modules/ingestion/`** from `modules/activities/` if the staging/promotion surface grows a second consumer. — CLAUDE.md §4.
- **Scheduling (cron) of ingestion/recheck runs.** MVP is user-launched slash commands only; no automatic trigger. — spec §11.
- **Per-category feed section overrides.** The declarative section system drives Category pages and Home over a partitioned pool — `shared/presets/FEED_SECTIONS.ts` (`DEFAULT_FEED_SECTIONS`) + `src/modules/feed/web/SectionedFeed.tsx` (one renderer) + `buildFeedSections`. The dev-only `design-showcase` route and the legacy `HeroActivityCard`/`ClassActivityCard` are now **deleted** (SilverBlackMode reskin, D10). What remains deferred: **per-category** section overrides (today one shared list serves all). — `src/shared/presets/FEED_SECTIONS.ts`.
- **`getCity(slug)` MCP tool / real multi-city.** Scouts derive the search term by de-slugifying and rely on the MCP `REJECTED`/bbox loop for coord validity; a `getCity` tool would give better search terms + first-try coords once multi-city exists. — spec §9.1.
- **Favorites keeps the static `PageHero` band, not the `FeaturedHero` carousel.** Home + category pages show the full-bleed featured-activity carousel; favorites is a personal saved list where "featured" doesn't apply, so it keeps the single-image `PageHero`. Give it a carousel (e.g. a saved-items rotation) only if the page ever wants one. — `src/app/(with-sidebar)/favorites/page.tsx`, `src/modules/activities/web/FeaturedHero.tsx`.
- **Promote the favorites hero band to config.** The favorites full-bleed `PageHero` (eyebrow/title/subtitle/image + "Discover more" action) is inlined in the page since it's the only static-band product consumer (design-showcase aside). Lift it into `FAVORITES_PRESET` (or a shared hero-band shape on `PagePreset`) if a second consumer appears. — `src/app/(with-sidebar)/favorites/page.tsx`, `src/shared/ui/PageHero.tsx`.
- **`FilterBarHorizontal`'s `horizontal` orientation is unused.** `TopFilters` always renders `orientation="rail"` (the floating left rail); the `horizontal`/`.filter-bar-top` branch is kept as the default for completeness. Drop it if no horizontal bar ever returns. — `src/modules/filters/web/FilterBar/FilterBarHorizontal.tsx`.
- **Chat graph is a single `model` node.** `buildChatGraph` compiles a one-node `StateGraph` (no tools). Add a `tools` node + a conditional `model → tools → model` edge with `model.bindTools(...)` once web-search / controlled DB-read tools exist — that's the whole point of the LangGraph base. — `src/modules/chat/application/chatGraph.ts`.
- **Chat returns no real activity suggestions.** The assistant is a pure chatbot with no DB access; `suggestedActivities` is always `[]` and the old hardcoded cards were removed. Wire a feed-read tool (via the graph above) when the assistant should surface real activities. — `src/modules/chat/web/chatMessagesRoute.ts`, `src/modules/chat/web/ChatPage.tsx`.
- **Chat stream phases are `'thinking' | 'writing'` only.** Every pre-answer-text latency (model warm-up, and — once `CHAT_THINKING_EFFORT` is on or a tools node lands — reasoning / tool calls, which emit no answer token) collapses to `'thinking'`. Add distinct phases (`'searching'`, `'reasoning'`, …) + their loader labels when those signals exist, so the UI can say "recherche sur le web…" rather than the generic bloom. — `src/shared/contracts/ChatStreamEvent.ts`, `src/modules/chat/web/ChatStatusIndicator.tsx`, `src/modules/chat/application/SendChatMessageUseCase.ts`.
- **Reasoning text is not surfaced.** When reasoning is enabled, the model streams `additional_kwargs.reasoning_content` deltas with empty `content`; `executeStream` ignores them (the loader just stays `'thinking'`). Surface a collapsible "réflexion" panel if showing the chain-of-thought is ever wanted. — `src/modules/chat/application/SendChatMessageUseCase.ts`.
- **`DotMatrixLoader` lives in `chat/web` (single consumer).** It's a DTO-free primitive but only chat uses it; per the placement rule it stays in the capability until a second consumer appears, then promote to `shared/ui`. — `src/modules/chat/web/DotMatrixLoader.tsx`.
- **The "Passer à Premium" / "Découvrir Premium" buttons are inert.** Premium is toggled manually in DB/seed (`User.isPremium`); there is no payment flow and the modal CTAs just close. Wire a real upgrade path when billing exists. — `src/shared/ui/Premium.tsx`, `prisma/seed.ts`.

## Hardcoded

- **Home hero eyebrow `"THIS WEEK IN MONTREAL"`** is inlined in the home page. Category heroes use `CATEGORY_PRESETS[key].eyebrow`; home has no preset, so the eyebrow is a literal. Lift to a `HOME_PRESET` field if home copy becomes configurable. — `src/app/(with-sidebar)/page.tsx`, `FeaturedHero`.
- **`FeaturedHero` slide cap = 3, image-bearing only.** Category pages feed it the top 3 of the pool that have an `imageUrl` and exclude those ids from the feed below; home feeds the global featured pool. Slides with no photo are skipped. Tune the cap / selection if the curation rule changes. — `src/modules/feed/web/CategoryFeedPage.tsx`, `src/modules/activities/web/FeaturedHero.tsx`.
- **Filter rail reserves a 268px left column ≥1200px and collapses to a drawer below.** The fixed rail lives in the left gutter; `.shell { padding-left: 268px }` keeps content clear on desktop, and under 1200px it becomes a slide-in drawer behind a floating "Filters" toggle. Revisit breakpoints if the nav/rail width changes. — `src/app/globals.css` `.filter-rail`, `src/modules/filters/web/TopFilters.tsx`.
- **`RECHECK_INTERVAL_DAYS = 90`** for PLACE re-verification cadence. — `src/modules/activities/domain/freshness.ts`, spec §6.
- **Default city slug `'montreal'`** in the admin create route when `citySlug` is omitted. — `adminActivityRoute.ts`.
- **No GIN index on `Activity.categories Json`.** The feed query ORs JSON-path filters (`primary equals` / `secondary array_contains`); fine at POC scale. Add a GIN index (and revisit the where-clause) if data grows. — `PrismaActivityRepository.findCandidates`, `prisma/schema.prisma`, spec §4.
- **`SPORT` is absent from `CATEGORY_PRESETS`** (5 keys, no `sport`); SPORT lives in a separate `SPORT_PRESET` + `/sport` page. Flagged, not actioned — surfaced for the user. — spec §0 field audit.
- **`STAGED_DEDUPE_KEY = 'pending-promotion'`** written by `ingestActivity` as the candidate's staging dedupeKey; the authoritative key is recomputed inside `PromoteCandidateUseCase` (cannot be computed at staging — `computeDedupeKey` throws for an EVENT without dateStart). — `src/mcp/tools/ingestActivity.ts`, spec plan-2 §8.
- **`.mcp.json` uses a Windows `cmd /c pnpm` launcher.** On POSIX, drop `"cmd", "/c"` and set `"command": "pnpm"`. — `.mcp.json`.
- **`pnpm.overrides` pins `better-call` to `1.3.5`.** `better-auth@1.6.11` + `@better-auth/core` otherwise let pnpm peer-resolution pull an older `better-call@1.1.8` (missing `kAPIErrorHeaderSymbol`), which breaks the Next build. Drop the override once Better Auth ships a self-consistent dependency tree. — `package.json`.
- **`PLACEHOLDER_IMAGE_URL = '/placeholder-activity.svg'`** rendered by every card + the modal when an activity has no image. `imageUrl` is now nullable end-to-end (MCP schema, `ExtractedActivityPayload`, `Activity`, Prisma `String?`, `ActivityDTO`, admin route). The fallback lives in the web layer (`coverImageUrl` in `cards/helpers.ts`), so the DTO stays truthfully nullable. Swap the static SVG for a real placeholder asset / per-category treatment if richer "no image" UX is ever wanted. — `src/modules/activities/web/cards/helpers.ts`, `public/placeholder-activity.svg`.
- **Feed-section tuning constants** in `src/modules/feed/web/buildFeedSections.ts`: `POOL_LIMIT = 48` (page pool size, cap `MAX_FEED_LIMIT = 50`), `MIN_SECTION_ITEMS = 6` (themed-section auto-hide floor), `MAX_SECTION_ITEMS = 11` (feature + 2 stanzas). Plus `MARKER_CAP = 24` in `src/modules/activities/web/MapSection.tsx`. Tune when the catalogue grows. — spec `2026-06-01-sectioned-activity-feed-design.md`.
- **`--live` (#D8453F) is the only functional colour accent.** The Quiet Luxury theme is otherwise strictly monochrome (charcoal/silver on off-white); a single restrained red is reserved for the LIVE badge as an "in-progress" signal. Keep new UI monochrome unless it's a genuine functional signal. — `src/app/globals.css` `:root --live`, `.cover-card-live`.
- **Favorites hero image URL (Unsplash) hardcoded inline.** `photo-1444723121867-…` is referenced directly on the favorites `PageHero`; swap for a curated/owned Montréal asset when one exists. — `src/app/(with-sidebar)/favorites/page.tsx`.
- **Scroll-effect tuning constants.** Hero parallax factor `0.28` (clamped to `700px` of scroll) in `FeaturedHero`; rail reveal threshold `window.innerHeight * 0.3` and reveal slide `28px` for the `.filter-rail`; Lenis `lerp: 0.1`. The map explorer (`.map-explorer-map`) sets `data-lenis-prevent-wheel` only while its volet is open, so the wheel zooms the map only when it's "active" and otherwise scrolls the page. Tuned by feel; adjust together if the hero height or rail position changes. — `src/modules/activities/web/FeaturedHero.tsx`, `src/modules/filters/web/TopFilters.tsx`, `src/modules/activities/web/MapSection.tsx`, `src/app/globals.css`, `src/shared/ui/SmoothScroll.tsx`.
- **SilverBlackMode reskin tokens live in `globals.css :root`.** The 2026-06 design handoff added `--ink-*`, `--smoke-2/3`, `--silver-100..500`, `--paper`, `--white`, `--line-dark*`, `--brushed*`, `--panel`, `--shadow-xs`, `--ease`, `--font`. The pre-existing near-identical names (`--smoke`, `--line`, `--line-2`, `--shadow-sm|md|lg`, `--offwhite`) intentionally kept their original values rather than the prototype's, so the earlier hero + filter rail were not disturbed. Reconcile to one palette if the older `--charcoal-*`/`--silver`/`--silver-2` names are ever retired. — `src/app/globals.css` `:root`.
- **Premium band/modal copy, price, and AI-companion preview are static POC placeholders.** `9,99 $/mois`, the three feature blurbs, the mini-chat conversation, the suggested "Belvédère du Mont-Royal" pick (+ its inline Unsplash image), and the daily-tracking row are all hardcoded; the `SiteFooter` social links (`instagram`/`facebook`/`x`) are inert `#` anchors. Wire to real plans/links when Premium and social presence exist. — `src/shared/ui/Premium.tsx`, `src/shared/ui/SiteFooter.tsx`.
- **Imageless-card French category labels live in `cards/categoryMeta.ts`.** `CATEGORY_OPTIONS` (`filters/web`) holds English labels, which would violate the French decision (D3), so the no-photo card maps `ActivityCategory → FR label` locally (`Sport`/`Gastronomie`/`Vie nocturne`/…) alongside the design icon map. Reconcile if a shared i18n category-label source ever exists. — `src/modules/activities/web/cards/categoryMeta.ts`.
- **`OLLAMA_MODEL='gemma4:12b'` is the real pulled tag; `OPENROUTER_MODEL='google/gemma-3-12b-it'` is still a placeholder.** Set the OpenRouter slug to the real one before relying on chat via that provider. — `src/shared/config/env.ts`.
- **Ollama token counting is unreliable.** `usage_metadata` (`input_tokens`/`output_tokens`/`total_tokens`) is sometimes absent on Ollama's OpenAI-compatible endpoint depending on version; `SendChatMessageUseCase` falls back to `0`, so the monthly cap may under-count on Ollama. Revisit (e.g. a tokenizer-based estimate) if precise accounting matters. — `src/modules/chat/application/SendChatMessageUseCase.ts`.
- **Chat-gate lock styling is inline.** `ChatGate` blurs the page with inline styles (`filter: blur(7px)`, `opacity`, `pointer-events:none`) and a fixed-centred reopen CTA rather than a CSS class. Promote to `globals.css` if the locked-overlay treatment is reused. — `src/modules/chat/web/ChatGate.tsx`.
