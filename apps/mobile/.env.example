# Base URL of the Next.js API (use your LAN IP with `pnpm dev:lan` at the repo root
# so a device or simulator on the same network can reach it, e.g. http://192.168.1.10:3000)
EXPO_PUBLIC_API_URL=http://localhost:3000
